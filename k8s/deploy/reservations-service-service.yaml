apiVersion: v1
kind: Service
metadata:
  name: reservations-service-service
spec:
  selector:
    app: reservations-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP

