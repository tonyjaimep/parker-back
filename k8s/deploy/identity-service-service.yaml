apiVersion: v1
kind: Service
metadata:
  name: identity-service
spec:
  selector:
    app: identity-service
  ports:
    - name: identity-service
      port: 3000
      targetPort: 3000
  type: ClusterIP

